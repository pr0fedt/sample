/* @flow */

const config = {
  'HOSTNAME': {
    name: 'host',
    default: '0.0.0.0',
    desc: 'hostname to listen to'
  },

  'PORT': {
    name: 'portno',
    default: '3000',
    desc: 'port to bind to'
  }
};

export default config;
